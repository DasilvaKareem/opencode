---
description: Create a new raylib 3D game
agent: raylib
---

Create a new 3D game using raylib with the following structure:

1. Create `{{name}}.c` with:
   - Window initialization
   - 3D camera setup (first-person or third-person)
   - Basic 3D models (cubes, spheres, or custom)
   - Camera controls
   - Lighting setup
   - Ground/floor rendering
   - FPS display

2. Include raylib.h, raymath.h, and rlgl.h if needed
3. Add comments explaining 3D concepts
4. Create a Makefile for compilation

Game name: {{name}}
Description: {{description}}
Camera type: {{camera_type}}
